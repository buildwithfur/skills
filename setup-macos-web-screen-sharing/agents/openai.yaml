interface:
  display_name: "macOS Web Screen Sharing"
  short_description: "Hand private user input to a real Mac session"
  default_prompt: "Use $setup-macos-web-screen-sharing when my task requires private or manual input in my existing Mac session."
