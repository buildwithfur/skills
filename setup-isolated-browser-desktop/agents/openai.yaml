interface:
  display_name: "Isolated Browser Desktop"
  short_description: "Hand private user input to a shared browser"
  default_prompt: "Use $setup-isolated-browser-desktop when my task requires private or manual input in the agent's browser session."
